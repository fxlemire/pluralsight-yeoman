import {assert, helpers} from 'yeoman-generator';
import path from 'path';

describe('yang:app', () => {
  describe('default', () => {
    before(() => {

    });

    it('creates files', () => {

    });

    it('adds default ngapp', () => {

    });
  });
});
